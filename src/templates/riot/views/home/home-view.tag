<home-view>

    <div>Test</div>

    <script>
        import EventBroker from 'structure-js/js/event/EventBroker';
console.log(`EventBroker`, EventBroker);
        //--------------------------------------------------------------------------------
        // PROPERTIES
        //--------------------------------------------------------------------------------

        //--------------------------------------------------------------------------------
        // METHODS
        //--------------------------------------------------------------------------------
        this.refresh = () => {

            this.update();
        };

        this.onChange = event => {
            this.refresh();
        };

        //--------------------------------------------------------------------------------
        // LISTENERS
        //--------------------------------------------------------------------------------
        this.on('mount', () => {
            this.refresh();
        });

        this.on('unmount', () => {
        });
    </script>
</home-view>
